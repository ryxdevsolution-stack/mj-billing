{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0 --workers 4 backend.app:app",
    "restartPolicyType": "always"
  }
}
